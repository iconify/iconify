"use strict";

if (self.SimpleSVG === void 0) {
    self.SimpleSVG = {
        isReady: false
    };
    (function (SimpleSVG, global) {
        var local = {
            config: {}
        };

        /* content */

    })(self.SimpleSVG, self);
}
