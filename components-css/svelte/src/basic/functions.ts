// Re-export functions used in component
import { renderContent } from '@iconify/component-utils/helpers/content';
import { getSizeProps } from '@iconify/component-utils/helpers/size';

export { renderContent, getSizeProps };
