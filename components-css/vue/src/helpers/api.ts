import { setFetch, fetchJSON } from '@iconify/fetch';
import { createIconifyAPILoader } from '@iconify/component-utils/loader/api/create';
import {
	setLoader,
	setProviderLoader,
	getLoader,
} from '@iconify/component-utils/loader/loaders';
import type { LoaderConfig } from '@iconify/component-utils/loader/types';

export {
	setFetch,
	fetchJSON,
	createIconifyAPILoader,
	setLoader,
	setProviderLoader,
	getLoader,
};

export type { LoaderConfig };
